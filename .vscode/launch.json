{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/build/vulkan-tutorial",
            "args": [],
            "preLaunchTask": "Build Project",
            "cwd": "${workspaceFolder}"
        }
    ]
}
